We are given Minimum Spanning Tree of a graph. It's needed to update the MST when a new
edge is added without creating the whole MST again. Note that we do not apply an algorithm
to create MST, (like Kruskal's Algorithm) since MST already exists.